<UserControl x:Class="EntityGeneratorWindows.UserControls.Loading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="80" Background="#FF272727">
    <Grid>
        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0"
                             IsIndeterminate="True" Height="80" Width="80" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBlock x:Name="textBlock" 
                   Margin="14,32,14,32" 
                   TextWrapping="Wrap" 
                   Text="载入中..." 
                   FontWeight="Bold" 
                   FontSize="13" 
                   Foreground="#FFD32F2F" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
